{
  "name": "x-mutual-marker",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that labels mutual and one-way follow relationships on x.com with optional follow-rate badges.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
